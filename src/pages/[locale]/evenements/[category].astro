---
// Events Category Page
import Layout from '../../../layouts/Layout.astro';
import Navigation from '../../../components/Navigation.astro';
import { loadTranslations, t, locales } from '../../../utils/i18n';
import type { Locale } from '../../../types/i18n';

export async function getStaticPaths() {
  const categories = [
    'festivals',
    'concerts',
    'expositions',
    'marches',
    'sports',
    'animations'
  ];
  
  const paths = [];
  
  for (const localeConfig of locales) {
    for (const category of categories) {
      paths.push({
        params: { 
          locale: localeConfig.code,
          category: category
        },
      });
    }
  }
  
  return paths;
}

const { locale, category } = Astro.params;
const translations = await loadTranslations(locale as Locale);

// Mock events data
const allEvents = [
  {
    id: 1,
    name: "Festival International du Film d'Animation",
    slug: "festival-international-film-animation",
    category: "festivals",
    description: "Le plus grand festival de films d'animation au monde se déroule chaque année à Annecy.",
    date: new Date("2024-06-10"),
    time: "14:00",
    location: "Bonlieu Scène Nationale",
    price: 15,
    image: "/images/events/festival-animation.jpg",
    tags: ["cinéma", "animation", "international"],
    isFeatured: true
  },
  {
    id: 2,
    name: "Concert de Jazz au Château",
    slug: "concert-jazz-chateau",
    category: "concerts",
    description: "Concert de jazz dans un cadre historique exceptionnel.",
    date: new Date("2024-07-15"),
    time: "20:30",
    location: "Château d'Annecy",
    price: 25,
    image: "/images/events/concert-chateau.jpg",
    tags: ["jazz", "musique", "château"],
    isFeatured: false
  },
  {
    id: 3,
    name: "Exposition Peinture Contemporaine",
    slug: "exposition-peinture-contemporaine",
    category: "expositions",
    description: "Découvrez les œuvres d'artistes locaux et internationaux.",
    date: new Date("2024-08-01"),
    time: "10:00",
    location: "Musée-Château d'Annecy",
    price: 8,
    image: "/images/events/expo-peinture.jpg",
    tags: ["art", "peinture", "culture"],
    isFeatured: false
  },
  {
    id: 4,
    name: "Marché de Noël d'Annecy",
    slug: "marche-noel-annecy",
    category: "marches",
    description: "Marché traditionnel avec artisanat local et spécialités savoyardes.",
    date: new Date("2024-12-01"),
    time: "10:00",
    location: "Vieille Ville d'Annecy",
    price: 0,
    image: "/images/events/marche-noel.jpg",
    tags: ["noël", "artisanat", "tradition"],
    isFeatured: true
  },
  {
    id: 5,
    name: "Marathon du Lac d'Annecy",
    slug: "marathon-lac-annecy",
    category: "sports",
    description: "Course autour du magnifique lac d'Annecy pour tous les niveaux.",
    date: new Date("2024-05-20"),
    time: "08:00",
    location: "Tour du Lac d'Annecy",
    price: 35,
    image: "/images/events/marathon-lac.jpg",
    tags: ["marathon", "course", "lac"],
    isFeatured: false
  },
  {
    id: 6,
    name: "Animations Enfants au Parc",
    slug: "animations-enfants-parc",
    category: "animations",
    description: "Spectacles et ateliers pour les enfants dans les parcs de la ville.",
    date: new Date("2024-07-20"),
    time: "15:00",
    location: "Parc de l'Europe",
    price: 0,
    image: "/images/events/animations-enfants.jpg",
    tags: ["enfants", "famille", "gratuit"],
    isFeatured: false
  }
];

// Category mapping
const categoryMap: Record<string, string> = {
  'festivals': 'Festivals',
  'concerts': 'Concerts',
  'expositions': 'Expositions',
  'marches': 'Marchés',
  'sports': 'Sports',
  'animations': 'Animations'
};

const categoryName = categoryMap[category as string] || category;
const filteredEvents = allEvents.filter(event => event.category === category);

const title = `${categoryName} - ${t(translations, 'nav.evenements')} - Salut Annecy`;
const description = `Découvrez tous les ${categoryName.toLowerCase()} à Annecy et dans la région.`;
---

<Layout 
  title={title}
  description={description}
  locale={locale as Locale}
>
  <Navigation slot="header" locale={locale as Locale} currentPath="/evenements" />
  
  <div class="container">
    <section class="category-hero">
      <nav class="breadcrumb">
        <a href={`/${locale}/evenements`}>{t(translations, 'nav.evenements')}</a>
        <span class="separator">›</span>
        <span class="current">{categoryName}</span>
      </nav>
      
      <h1 class="page-title">{categoryName}</h1>
      <p class="page-description">
        {filteredEvents.length} {filteredEvents.length === 1 ? 'événement' : 'événements'} dans cette catégorie
      </p>
    </section>
    
    <div class="events-layout">
      <main class="events-main">
        {filteredEvents.length > 0 ? (
          <div class="events-grid">
            {filteredEvents.map((event) => (
              <article class="event-card">
                {event.isFeatured && (
                  <div class="featured-badge">⭐ À la une</div>
                )}
                
                <div class="event-image">
                  <img src={event.image} alt={event.name} loading="lazy" />
                  <div class="event-date">
                    <span class="date-day">{event.date.getDate()}</span>
                    <span class="date-month">
                      {event.date.toLocaleDateString('fr-FR', { month: 'short' })}
                    </span>
                  </div>
                </div>
                
                <div class="event-content">
                  <h3 class="event-name">
                    <a href={`/${locale}/evenements/${category}/${event.slug}`}>
                      {event.name}
                    </a>
                  </h3>
                  <p class="event-description">{event.description}</p>
                  
                  <div class="event-details">
                    <div class="detail-item">
                      <span class="detail-icon">📅</span>
                      <span class="detail-text">
                        {event.date.toLocaleDateString('fr-FR')} à {event.time}
                      </span>
                    </div>
                    <div class="detail-item">
                      <span class="detail-icon">📍</span>
                      <span class="detail-text">{event.location}</span>
                    </div>
                    <div class="detail-item">
                      <span class="detail-icon">💰</span>
                      <span class="detail-text">
                        {event.price === 0 ? 'Gratuit' : `${event.price}€`}
                      </span>
                    </div>
                  </div>
                  
                  <div class="event-tags">
                    {event.tags.slice(0, 3).map((tag) => (
                      <span class="tag">{tag}</span>
                    ))}
                  </div>
                  
                  <div class="event-actions">
                    <a href={`/${locale}/evenements/${category}/${event.slug}`} class="btn btn-primary">
                      Voir les détails
                    </a>
                    {event.price > 0 && (
                      <button class="btn btn-ghost">
                        Réserver
                      </button>
                    )}
                  </div>
                </div>
              </article>
            ))}
          </div>
        ) : (
          <div class="no-events">
            <h3>Aucun événement trouvé</h3>
            <p>Il n'y a pas d'événements programmés dans cette catégorie pour le moment.</p>
            <a href={`/${locale}/evenements`} class="btn btn-primary">
              Voir tous les événements
            </a>
          </div>
        )}
      </main>
      
      <aside class="events-sidebar">
        <div class="sidebar-widget card">
          <div class="card-body">
            <h3 class="widget-title">Catégories</h3>
            <ul class="category-list">
              <li><a href={`/${locale}/evenements/festivals`} class={category === 'festivals' ? 'active' : ''}>🎭 Festivals</a></li>
              <li><a href={`/${locale}/evenements/concerts`} class={category === 'concerts' ? 'active' : ''}>🎵 Concerts</a></li>
              <li><a href={`/${locale}/evenements/expositions`} class={category === 'expositions' ? 'active' : ''}>🎨 Expositions</a></li>
              <li><a href={`/${locale}/evenements/marches`} class={category === 'marches' ? 'active' : ''}>🛍️ Marchés</a></li>
              <li><a href={`/${locale}/evenements/sports`} class={category === 'sports' ? 'active' : ''}>⚽ Sports</a></li>
              <li><a href={`/${locale}/evenements/animations`} class={category === 'animations' ? 'active' : ''}>🎪 Animations</a></li>
            </ul>
          </div>
        </div>
        
        <div class="sidebar-widget card">
          <div class="card-body">
            <a href={`/${locale}/evenements`} class="btn btn-ghost btn-block">
              ← Tous les événements
            </a>
          </div>
        </div>
      </aside>
    </div>
  </div>
</Layout>

<style>
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1rem;
  }

  .category-hero {
    padding: 2rem 0;
    border-bottom: 1px solid #e5e7eb;
    margin-bottom: 2rem;
  }

  .breadcrumb {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 1rem;
    font-size: 0.875rem;
    color: #6b7280;
  }

  .breadcrumb a {
    color: #8b5cf6;
    text-decoration: none;
  }

  .separator {
    color: #d1d5db;
  }

  .current {
    font-weight: 600;
  }

  .page-title {
    font-size: 2.5rem;
    font-weight: 700;
    color: #111827;
    margin-bottom: 0.5rem;
  }

  .page-description {
    font-size: 1.125rem;
    color: #6b7280;
  }

  .events-layout {
    display: grid;
    grid-template-columns: 1fr 300px;
    gap: 2rem;
    margin-bottom: 4rem;
  }

  .events-grid {
    display: grid;
    gap: 1.5rem;
  }

  .event-card {
    background: white;
    border-radius: 0.75rem;
    box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1);
    overflow: hidden;
    position: relative;
    display: grid;
    grid-template-columns: 250px 1fr;
    transition: box-shadow 0.2s;
  }

  .event-card:hover {
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
  }

  .featured-badge {
    position: absolute;
    top: 1rem;
    right: 1rem;
    background: #f59e0b;
    color: white;
    padding: 0.25rem 0.75rem;
    border-radius: 9999px;
    font-size: 0.75rem;
    font-weight: 600;
    z-index: 10;
  }

  .event-image {
    position: relative;
    height: 200px;
    overflow: hidden;
  }

  .event-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .event-date {
    position: absolute;
    top: 1rem;
    left: 1rem;
    background: rgba(0, 0, 0, 0.8);
    color: white;
    padding: 0.5rem;
    border-radius: 0.5rem;
    text-align: center;
    min-width: 60px;
  }

  .date-day {
    display: block;
    font-size: 1.5rem;
    font-weight: 700;
    line-height: 1;
  }

  .date-month {
    display: block;
    font-size: 0.75rem;
    text-transform: uppercase;
  }

  .event-content {
    padding: 1.5rem;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
  }

  .event-name {
    margin: 0 0 0.75rem 0;
  }

  .event-name a {
    color: #111827;
    text-decoration: none;
    font-size: 1.25rem;
    font-weight: 600;
    line-height: 1.4;
  }

  .event-name a:hover {
    color: #8b5cf6;
  }

  .event-description {
    color: #6b7280;
    line-height: 1.5;
    margin-bottom: 1rem;
    font-size: 0.875rem;
  }

  .event-details {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    margin-bottom: 1rem;
  }

  .detail-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .detail-icon {
    font-size: 0.875rem;
  }

  .detail-text {
    color: #374151;
    font-size: 0.875rem;
  }

  .event-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-bottom: 1rem;
  }

  .tag {
    background: #f3f4f6;
    color: #374151;
    padding: 0.25rem 0.5rem;
    border-radius: 0.375rem;
    font-size: 0.75rem;
  }

  .event-actions {
    display: flex;
    gap: 0.75rem;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.5rem 1rem;
    border-radius: 0.375rem;
    text-decoration: none;
    font-weight: 500;
    transition: all 0.2s;
    border: none;
    cursor: pointer;
    font-size: 0.875rem;
  }

  .btn-primary {
    background: #8b5cf6;
    color: white;
  }

  .btn-primary:hover {
    background: #7c3aed;
  }

  .btn-ghost {
    background: transparent;
    color: #8b5cf6;
    border: 1px solid #8b5cf6;
  }

  .btn-ghost:hover {
    background: #8b5cf6;
    color: white;
  }

  .btn-block {
    width: 100%;
  }

  .no-events {
    text-align: center;
    padding: 4rem 2rem;
  }

  .no-events h3 {
    font-size: 1.5rem;
    color: #374151;
    margin-bottom: 1rem;
  }

  .no-events p {
    color: #6b7280;
    margin-bottom: 2rem;
  }

  .events-sidebar {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .sidebar-widget {
    background: white;
    border-radius: 0.75rem;
    box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1);
  }

  .card-body {
    padding: 1.5rem;
  }

  .widget-title {
    font-size: 1.125rem;
    font-weight: 600;
    color: #111827;
    margin-bottom: 1rem;
  }

  .category-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .category-list li {
    margin-bottom: 0.5rem;
  }

  .category-list a {
    color: #6b7280;
    text-decoration: none;
    padding: 0.5rem;
    border-radius: 0.375rem;
    display: block;
    transition: all 0.2s;
  }

  .category-list a:hover,
  .category-list a.active {
    background: #f3f4f6;
    color: #8b5cf6;
  }

  @media (max-width: 768px) {
    .events-layout {
      grid-template-columns: 1fr;
    }
    
    .event-card {
      grid-template-columns: 1fr;
    }
    
    .event-image {
      height: 150px;
    }
    
    .page-title {
      font-size: 2rem;
    }
    
    .event-actions {
      flex-direction: column;
    }
  }
</style>